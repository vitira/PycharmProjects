# 4.5 Summing a million: Make a list from one to one million, and then use min() and max() to make sure your list
# actually starts at one and end at one million. Also use the sum() function to see how quickly Python can add a million
# numbers.
number = range(1, 1000001)
print(min(number))
print(max(number))
print(sum(number))
print(min)